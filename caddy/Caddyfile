localhost:8443 {
    handle /api/* {
      reverse_proxy backend:8080 # if running containerized
      # reverse_proxy host.docker.internal:8080 # if running locally
    }

    handle {
        root * /srv
        file_server
    }
}
